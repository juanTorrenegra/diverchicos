/// Shared logical resolution for all SALUD Flame layers (intro + bath if needed).
const double kSaludCowLogicalWidth = 1920;
const double kSaludCowLogicalHeight = 1080;

/// Default on-screen square size for cow / cat in SALUD intro (logical px).
const double kSaludAnimalDrawSizePx = 800;

const double kSaludCowAnimStepTime = 1 / 24;
